/*
1. What are the HTTP methods ?
Ans. HTTP (Hyper Text Transfer Protocol) defines several methods, also known as
    HTTP verbs, that indicate the desired action to be performed on a resource.
        These HTTP methods allow clients / web browsers to communicate with
    servers and perform operations on resources.

    Most commonly used HTTP methods:
    GET
    POST
    PUT
    DELETE
    PATCH
    OPTIONS
    HEAD



2. Difference between POST, PUT and PATCH method ?
Ans. POST method is always use for creating a resource (does not matter if it was duplicated).

    PUT method is checking resource if resource exists then update else create new resource.

    PATCH is always use for updating a resource.
*/